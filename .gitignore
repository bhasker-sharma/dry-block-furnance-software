# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class

# ── Virtual environment ────────────────────────────────────────────
venv/
.venv/

data/*.db
data/settings.json

# ── Calibration reports (customer data — must not be pushed) ───────
reports/*.json

# ── Company logo (per-lab upload, not fixed across installs) ───────
customer_assets/

# ── PyInstaller / build output ──────────────────────────────────────
build/
dist/
*.spec

# ── Inno Setup installer output ──────────────────────────────────────
installer_dist/

# ── Logs ─────────────────────────────────────────────────────────────
*.log

# ── Editor / IDE ─────────────────────────────────────────────────────
.vscode/
.idea/

# ── OS files ─────────────────────────────────────────────────────────
Thumbs.db
.DS_Store
internal_reference/